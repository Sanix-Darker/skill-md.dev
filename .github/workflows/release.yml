name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    name: Build and Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Run tests
        run: go test -v ./...

      - name: Build binaries
        run: |
          mkdir -p dist

          # Linux AMD64
          GOOS=linux GOARCH=amd64 go build -ldflags="-s -w -X github.com/sanixdarker/skillf/internal/cli.Version=${{ github.ref_name }} -X github.com/sanixdarker/skillf/internal/cli.Commit=${{ github.sha }}" -o dist/skillf-linux-amd64 ./cmd/skillf

          # Linux ARM64
          GOOS=linux GOARCH=arm64 go build -ldflags="-s -w -X github.com/sanixdarker/skillf/internal/cli.Version=${{ github.ref_name }} -X github.com/sanixdarker/skillf/internal/cli.Commit=${{ github.sha }}" -o dist/skillf-linux-arm64 ./cmd/skillf

          # macOS AMD64
          GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w -X github.com/sanixdarker/skillf/internal/cli.Version=${{ github.ref_name }} -X github.com/sanixdarker/skillf/internal/cli.Commit=${{ github.sha }}" -o dist/skillf-darwin-amd64 ./cmd/skillf

          # macOS ARM64 (Apple Silicon)
          GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w -X github.com/sanixdarker/skillf/internal/cli.Version=${{ github.ref_name }} -X github.com/sanixdarker/skillf/internal/cli.Commit=${{ github.sha }}" -o dist/skillf-darwin-arm64 ./cmd/skillf

          # Windows AMD64
          GOOS=windows GOARCH=amd64 go build -ldflags="-s -w -X github.com/sanixdarker/skillf/internal/cli.Version=${{ github.ref_name }} -X github.com/sanixdarker/skillf/internal/cli.Commit=${{ github.sha }}" -o dist/skillf-windows-amd64.exe ./cmd/skillf

      - name: Generate checksums
        run: |
          cd dist
          sha256sum * > checksums.txt

      - name: Generate changelog
        id: changelog
        run: |
          # Get the previous tag
          PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")

          if [ -n "$PREV_TAG" ]; then
            echo "## Changes since $PREV_TAG" > changelog.md
            echo "" >> changelog.md
            git log --pretty=format:"- %s (%h)" $PREV_TAG..HEAD >> changelog.md
          else
            echo "## Initial Release" > changelog.md
            echo "" >> changelog.md
            git log --pretty=format:"- %s (%h)" >> changelog.md
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          body_path: changelog.md
          files: |
            dist/skillf-linux-amd64
            dist/skillf-linux-arm64
            dist/skillf-darwin-amd64
            dist/skillf-darwin-arm64
            dist/skillf-windows-amd64.exe
            dist/checksums.txt
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
